
☕ Coffee Order System
    Проект демонстрирует микросервисную архитектуру для обработки заказов на кофе с использованием Spring Boot, RabbitMQ и базы данных.
    Система состоит из REST API, брокера сообщений и двух очередей: для новых заказов и для выполненных.

//////////////////////////////////////////////////////////////////////////////////////////////////////
📌 Архитектура
    📥 Пользователь → REST API → RabbitMQ → Consumer → ✔ Обработка заказа → 📤 Готовый заказ → Consumer → Сохранение в БД


🔄 Компоненты и их роли
//////////////////////////////////////////////////////////////////////////////////////////////////////
1. RabbitConfig.java — Конфигурация RabbitMQ
#Queue (Очередь):

    coffee-queue — основная очередь для новых заказов.

    completed-coffee-queue — очередь для готовых заказов.

#Exchange (Обменник):
    DirectExchange с именем coffee-exchange — отправляет сообщения в нужную очередь по ключу маршрутизации (routing key).

#Routing Keys:
    coffee-order — для новых заказов.

    completed-coffee-order — для завершённых заказов.

#Binding (Связь):

    Привязывает очередь к обменнику по ключу.

#RabbitTemplate:

    Упрощает отправку сообщений в RabbitMQ.

#Jackson2JsonMessageConverter:

    Конвертирует Java-объекты в JSON и обратно при передаче через RabbitMQ.

✅ Здесь ты настраиваешь инфраструктуру RabbitMQ.

//////////////////////////////////////////////////////////////////////////////////////////////////////
2. OrderController.java — REST-контроллер
    Обрабатывает HTTP-запросы по /orders/create и /orders/complete.

    Создаёт объект Order и сохраняет его в базу.

    Отправляет заказ в очередь через соответствующий Producer.

✅ Это Web-интерфейс твоей системы — входная точка.

//////////////////////////////////////////////////////////////////////////////////////////////////////
3. OrderProducer.java и CompletedOrderProducer.java — Публикаторы (Publisher)

    OrderProducer — отправляет заказ в очередь coffee-queue.
    CompletedOrderProducer — отправляет готовый заказ в очередь completed-coffee-queue.

✅ Они являются Publisher — отправляют сообщения в обменник.

//////////////////////////////////////////////////////////////////////////////////////////////////////
4. OrderConsumer.java и CompletedOrderConsumer.java — Обработчики (Consumer)
#OrderConsumer:
    Подписан на coffee-queue.
    Получает заказ, парсит JSON, сохраняет в базу, имитирует приготовление напитка.

#CompletedOrderConsumer:
    Подписан на completed-coffee-queue.
    Получает готовый заказ, сохраняет и выводит уведомление.
✅ Это Consumer — получают и обрабатывают сообщения из очереди.

//////////////////////////////////////////////////////////////////////////////////////////////////////
5. Order.java — Сущность (Entity)
    Представляет заказ в БД.
    Аннотирована @Entity и сохраняется через JPA.
✅ Это бизнес-модель.

//////////////////////////////////////////////////////////////////////////////////////////////////////
6. OrderDTO.java — Data Transfer Object
    Используется в контроллере для передачи данных от клиента.
✅ Это слой передачи данных (не привязан к БД).

//////////////////////////////////////////////////////////////////////////////////////////////////////
7. OrderPublisher.java — Альтернативный Publisher
    Простой компонент, который напрямую отправляет строку в RabbitMQ.
    Используется скорее для тестов или простых случаев.
